version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tic-tac-toe
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
